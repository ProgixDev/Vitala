vitala-prod.apk

# transient git worktrees created by Claude Code
.claude/worktrees/

# personal shift reports — the rest of docs/ is tracked
docs/shift-report-*.md

# macOS finder noise — tracked by accident, conflicts across machines
.DS_Store
